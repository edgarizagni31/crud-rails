<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid d-flex justify-content-between w-100 align-items-center">
    <div>
      <a class="navbar-brand" href="#">Todo app</a>
    </div>
    <ul class="navbar-nav">
      <li class = "nav-link <%= 'active' if current_page?(root_path) %>">
        <%= link_to "Bienvenido", root_path, {class: "text-decoration-none text-muted"} %>
      </li>
      <li class = "nav-link <%= 'active' if current_page?(tasks_path) %>">
        <%= link_to "Tareas", tasks_path, {class:  "text-decoration-none text-muted"}  %>
      </li>
      <li class = "nav-link <%= 'active' if current_page?( about_path) %>">
        <%= link_to "Sobre mi", about_path, {class:  "text-decoration-none text-muted"} %>
      </li>
      <li >
        <%= form_with url:destroy_user_session_path, method: :delete do |form| %>
          <% form.submit "Cerrar sesión", class: "btn btn-link" %>
        <%end %>
      </li>
    </ul>
  </div>
</nav>
